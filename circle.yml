## configuration file for Circle continuous integration

deployment:
  staging:
    branch: master
    heroku:
      appname: benefitmy-python-staging

database:
  override:
    - postgre -u ubuntu circle_test
